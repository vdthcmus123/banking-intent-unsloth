aug_rate: 0.35
configs_dir: /kaggle/working/configs
gradient_accumulation_steps: 8
learning_rate: 0.0001
load_in_4bit: true
logging_steps: 10
lora_alpha: 64
lora_bias: none
lora_dropout: 0.05
lora_r: 32
lr_scheduler_type: cosine
max_seq_length: 512
model_name: unsloth/Qwen2.5-7B-Instruct-bnb-4bit
num_epochs: 3
num_subset_intents: 20
optimizer: adamw_8bit
output_dir: /kaggle/working/banking-intent-lora
per_device_train_batch_size: 1
sample_data_dir: /kaggle/working/sample_data
target_modules:
- q_proj
- k_proj
- v_proj
- o_proj
- gate_proj
- up_proj
- down_proj
use_gradient_checkpointing: unsloth
warmup_ratio: 0.1
weight_decay: 0.01
